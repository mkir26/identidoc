input   {
    tcp {
        port => 5000
        codec => json
    }
    udp {
        port => 5000
        codec => json
    }
}

filter {
     
}

output  {
    elasticsearch {
        hosts => ["https://elasticsearch:9200"]
        ssl => true
        ssl_certificate_verification => false
        user => "elastic"
        password => "12345"
        cacert => ""
     }
    
    stdout {
        codec => rubydebug
        }
}